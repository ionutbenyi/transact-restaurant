database.ip = ${MYSQL_IP:localhost}
database.port = ${MYSQL_PORT:3306}
database.uname = ${MYSQL_USER:root}
database.password = ${MYSQL_PASSWORD:root}
database.name = ${MYSQL_DBNAME:restaurant-items-db}
database2.name = ${MYSQL_DBNAME:restaurant-staff-db}

#spring.datasource.url = jdbc:mysql://${database.ip}:${database.port}/${database.name}?allowPublicKeyRetrieval=true&useSSL=false&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
#spring.datasource.username =${database.uname}
#spring.datasource.password = ${database.password}
#
#spring.second-datasource.url = jdbc:mysql://${database.ip}:${database.port}/${database2.name}?allowPublicKeyRetrieval=true&useSSL=false&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
#spring.second-datasource.username = ${database.uname}
#spring.second-datasource.password = ${database.password}

#jdbc.driverClassName=org.h2.Driver
#restaurant-items-db.jdbc-url=jdbc:mysql://${database.ip}:${database.port}/${database.name}
#restaurant-staff-db.jdbc-url=jdbc:mysql://${database.ip}:${database.port}/${database2.name}
#jdbc.user=root
#jdbc.pass=root
#
## hibernate.X
#hibernate.dialect=org.hibernate.dialect.H2Dialect
#hibernate.show_sql=false
#hibernate.hbm2ddl.auto=create-drop
#hibernate.cache.use_second_level_cache=false
#hibernate.cache.use_query_cache=false

server.port=8081

hibernate.hbm2ddl.auto=create-drop
hibernate.cache.use_second_level_cache=false
hibernate.cache.use_query_cache=false

spring.datasource.jdbcUrl=jdbc:mysql://${database.ip}:${database.port}/${database.name}
spring.datasource.username=root
spring.datasource.password=root

spring.second-datasource.jdbcUrl=jdbc:mysql://${database.ip}:${database.port}/${database2.name}
spring.second-datasource.username=root
spring.second-datasource.password=root